# Same-origin path; the Vite dev proxy forwards /api/* to the upstream (vite.config.js).
VITE_CHECKOUT_SESSION_URL=/api/v1/checkout/sessions

# Merchant secret key, injected server-side by the dev proxy. No VITE_ prefix:
# it must never reach the browser bundle.
LITE_SECRET_KEY=
